import React from "react"
import "../index.scss"

const Main: React.FC = () => {
    return (
        <nav className="nav">
            <a href="#">Мои ссылки</a>
        </nav>
    )
}

export default Main
